## 1. 规范化栏目身份与迁移契约

- [x] 1.1 盘点栏目列表、当前选择、订阅、排序和导航中仍以裸 `ForumChannel.id` 或旧 `source:nativeKey` 判断身份的调用点，并用聚焦测试固定现有 NGA、V2EX、LINUX DO 行为
- [x] 1.2 建立来源作用域的规范化栏目身份，使 NGA 裸数字与 `fid:` 统一为 `nga:fid:<value>`、`stid:` 保持 `nga:stid:<value>`，其他来源继续使用 `source:nativeKey`
- [x] 1.3 将栏目行、首页横向导航、高亮和切换接缝改为显式使用规范化字符串身份，增加同值 `fid/stid` 不碰撞及旧 generation 不覆盖新栏目的测试

## 2. 订阅存储版本与安全迁移

- [x] 2.1 为订阅集合和顺序增加新版本存储格式，迁移旧 NGA 裸数字键并保持 V2EX、LINUX DO 现有键、有效订阅和相对顺序
- [x] 2.2 在迁移中按规范化身份去重、丢弃损坏记录并保证每个已启用来源至少保留一个有效栏目，覆盖默认恢复和排序持久化测试
- [x] 2.3 保留旧订阅与顺序快照而不覆盖，增加回滚旧二进制仍能读取普通栏目、且新 `stid` 不会被误解为 `fid` 的兼容测试

## 3. 统一栏目目录展示快照

- [x] 3.1 建立 Community 使用的只读栏目目录展示模型，承载规范化身份、浏览目标、标题、可选父版上下文、来源种类和可搜索信息，不解析 NGA 原始字段
- [x] 3.2 组合现有普通栏目与最近确认的网事杂谈权威直接子版，按规范化身份去重并优先保留权威标题与父版关系
- [x] 3.3 为 V2EX、LINUX DO 和无权威目录的 NGA 状态保留现有普通栏目行为，增加跨来源隔离、重复 fid、同值 fid/stid 和无目录测试
- [x] 3.4 将权威目录缓存恢复与刷新从当前 Feed 选中栏目解耦，在启动、NGA 来源切换和前台激活时为栏目目录提供最新安全快照

## 4. 权威子版独立订阅与动态变化

- [x] 4.1 允许栏目目录中的权威子版写入新版本订阅和排序，并让 `ForumSubscriptionStore` 以规范化身份恢复、添加、移除和重排
- [x] 4.2 将已订阅权威子版投影到首页顶部导航，验证添加 `[股市]技术分析` 后使用 `nga:stid:47206901` 出现并在重启后恢复
- [x] 4.3 复用现有 `ForumChannel` 浏览目标实现独立子版 Feed，验证 `stid` 使用 Web forum 路径、`fid` 使用现有路径且不加载网事杂谈主版
- [x] 4.4 让一次完整目录同步的新增、改名和取消变化可分别被筛选偏好与栏目订阅消费：新增不自动订阅、改名保留订阅、取消清理订阅和顺序并产生一次性提示
- [x] 4.5 覆盖请求、解码和结构失败保留最近确认子版与订阅，以及首次无缓存只显示普通栏目并报告子版暂不可更新的测试

## 5. 首页订阅与网事杂谈筛选隔离

- [x] 5.1 保持首页独立子版订阅和 `FeedPreferencesStore` 父版聚合选择使用两套持久化状态，删除或避免任何双向隐式写入
- [x] 5.2 增加添加/移除独立子版不改变网事杂谈筛选、应用筛选不改变首页订阅和顺序的状态测试
- [x] 5.3 增加同一子版同时独立订阅与聚合选择时两条入口加载不同 Feed 语义，以及权威取消后两套状态分别安全归一化的测试

## 6. 重写栏目页信息层级与交互

- [x] 6.1 将栏目页重组为标题与来源、统一搜索、“已添加到首页”和“浏览全部栏目”四个明确区域，并为 NGA 区分网事杂谈子版与其他栏目
- [x] 6.2 实现统一本地搜索，使标题、规范化编号和已有说明可匹配，并在子版结果中展示“网事杂谈 › 子版”上下文及无结果状态
- [x] 6.3 将栏目行主体的独立打开动作与添加/已添加控件分离，保留至少一个当前来源栏目并提供明确的状态反馈
- [x] 6.4 将拖动排序收进显式“编辑排序”状态，默认隐藏拖动手柄，并确保完成编辑后首页顶部顺序即时一致
- [x] 6.5 复用 `PaperTheme` 和现有 DesignSystem 完成加载、缓存、目录不可用、新增与取消状态样式，补齐稳定 accessibility identifier、最小点击区域、动态字体和 VoiceOver 标签

## 7. 验证与文档收口

- [x] 7.1 运行栏目身份、订阅迁移、目录组合、权威变化、筛选隔离和 Feed generation 聚焦测试，并修复所有回归
- [x] 7.2 在已连接 iOS 真机运行完整 `ForumHubTests` 和 Debug 构建；无可用真机时记录未执行且不得以模拟器替代等价验收
- [x] 7.3 在真机人工验证搜索并添加 `[股市]技术分析`、首页出现、重启恢复、排序、独立浏览、移除、网事杂谈筛选隔离、较大动态字体和 VoiceOver
- [x] 7.4 根据实际实现与验证结果更新 `docs/modules/community-management.md`、`docs/modules/forum-feed.md`、`docs/modules/nga.md`、`docs/context.md`、`CONTEXT.md`、`docs/decisions.md`、`docs/todo.md` 和 `docs/changelog.md`
- [x] 7.5 执行严格 Rasen validate 与 `git diff` 检查，确认没有无关重构、调试日志、临时文件或未经验证即勾选的待办
- [x] 7.6 修复验证报告 V-001：将现有 UI 测试的栏目定位同步到规范化 accessibility identifier，并在已连接真机重新运行受影响用例与完整测试门禁
