## 1. 固定真实子版合约

- [x] 1.1 在已登录真机“哥谭之王”上取得网事杂谈父版的当前 `__F/sub_forums` 响应，移除 Cookie、账号、帖子正文及无关字段后保存脱敏 Fixture，并记录获取入口与日期
- [x] 1.2 用 Fixture 确认直接子版边界、`fid/stid` 表达、过滤 ID、attributes 和实际浏览请求参数；任一关键字段无法确认时暂停实现并更新 design
- [x] 1.3 为有效结构、空子版、缺少父版身份、截断位置数组和未知字段建立 Parser 契约测试，确保不完整结构不会提交权威快照

## 2. NGA 权威子版目录

- [x] 2.1 在 NGA 适配范围建立区分 `fid`、`stid`、过滤 ID 与父版身份的来源模型，并向上层只暴露稳定键、标题和可浏览 `ForumChannel` 目标
- [x] 2.2 实现仅解析已验证父版 `__F/sub_forums` 容器的严格 Parser，不再从全站分类、收藏或标签栏目推断子版
- [x] 2.3 为 `ThreadRepository` 增加按父版加载子版目录的能力与 NGA 实现，复用现有登录 Cookie、取消和字符集处理；其他来源返回无能力而非伪造目录
- [x] 2.4 增加 fid/stid 数值碰撞、过滤 ID 不参与浏览、父版不匹配和权威请求失败的 Repository 测试

## 3. 快照、增删检测与偏好迁移

- [x] 3.1 增加按来源与父版隔离的最近确认目录快照，只在完整成功响应后原子替换并计算新增、改名和取消集合
- [x] 3.2 让首次成功同步建立无“全部新增”提示的基线；后续新增默认未选，改名保留选择，取消条目从有效选择移除并生成一次性提示
- [x] 3.3 将子版偏好升级为稳定键集合，旧 Int 选择仅在权威快照可唯一映射时迁移；目录失败时延迟迁移且不请求未经确认的栏目
- [x] 3.4 覆盖完整同步、部分/异常响应、首次基线、新增、改名、取消、延迟迁移和跨父版隔离测试

## 4. 主版与多子版聚合

- [x] 4.1 将 `ForumViewModel.availableChildChannels` 改为只消费网事杂谈权威目录，删除“排除当前栏目即视为子版”的全站扁平逻辑
- [x] 4.2 用稳定子版键恢复和归一化多选状态，保持默认空选择、主版始终包含，并让一次应用只触发一次 Feed reload
- [x] 4.3 调整聚合加载，使主版失败维持现有整体错误，而单个子版失败仍合并其他成功内容并产生可重试的局部失败状态
- [x] 4.4 保持 generation/取消、`source + tid` 去重、当前排序和远端分页语义，增加选择切换迟到响应、重复主题和后续页局部失败测试

## 5. 网事杂谈筛选体验

- [x] 5.1 将筛选 Sheet 的子版区改为“主版始终包含 + 真实子版多选”，提供“只看主版”、已选数量、草稿取消和一次应用
- [x] 5.2 为新增子版提供可定位的“新”状态并在用户查看后确认；全站无关栏目不得显示或计入筛选数量
- [x] 5.3 在没有确认快照时显示目录暂不可用，在取消已选子版或部分内容加载失败时提供简短中文提示且不清空已成功 Feed
- [x] 5.4 检查 VoiceOver 标签、动态字体、点击区域以及真实子版数量较多时的本地搜索可达性

## 6. 验证与文档

- [x] 6.1 运行 NGA 子版 Parser、Repository、偏好迁移、Feed generation、分页、去重和筛选状态相关单元测试（2026-07-22，Xcode Beta，全新 DerivedData，真机“哥谭之王” `00008150-001A4D5E1428401C`，39 tests / 4 suites，`TEST SUCCEEDED`；包含目录取消当前选择后一次 reload 与旧 generation 拒绝提交）
- [x] 6.2 在真机“哥谭之王”验证：用户人工确认主版始终包含/默认主版、多选应用、重开恢复、搜索、较大字体、VoiceOver、取消草稿和一次应用均“一切正常”；前台刷新、局部失败、新增/取消模拟由同机 Swift Testing 覆盖，不使用模拟器替代
- [x] 6.3 同步 `docs/modules/forum-feed.md`、`docs/modules/nga.md`、`docs/decisions.md`、`docs/changelog.md` 和受影响的 `docs/todo.md`
- [x] 6.4 执行 `git diff --check` 与严格 Rasen 校验，确认未调用 `nuke.php` 修改服务器端屏蔽、未改变其他来源能力且未遗留未脱敏响应或调试日志
