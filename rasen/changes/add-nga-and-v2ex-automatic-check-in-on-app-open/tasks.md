## 1. 真实响应采样与证据门禁

- [x] 1.1 在 Rasen `workDir/research/` 编写仅供本次变更使用的采样说明，明确授权账号、请求顺序、原始响应隔离位置、Cookie/Token/UID/用户名/余额/动态 `once` 脱敏规则和禁止提交项
- [ ] 1.2 使用已登录测试账号依次采集 NGA `get_stat` 未签到响应、一次 `check_in` 成功响应及随后 `get_stat` 已签到响应，并记录状态码、最终 URL、必要请求头、响应内容类型与服务端日期线索
- [ ] 1.3 使用已登录 V2EX Web Session 依次采集 `/mission/daily` 可领取页面、页面签发的领取动作、领取结果、再次访问的已领取页面及失效会话跳转，并记录最终 URL、重定向、内容类型和 UTC 周期证据
- [x] 1.4 在 `ForumHubTests/Fixtures` 下提交来源专属 Fixture：真实样本必须人工脱敏；暂缺真实样本的场景可使用明确标注为合成或公开文档驱动的 Fixture，不得冒充服务端证据；搜索确认不存在 Cookie、Token、真实 UID、用户名、余额和可重放动态值
- [ ] 1.5 真实采样完成后，根据真实 Fixture 校准 `design.md` 中 NGA/V2EX 状态机、成功判据、领取路径、日界线和请求约束，并解除对应来源的真实写入与最终验收门禁；若无法明确证明未完成状态，则该来源保持 fail-closed 且不得启用写操作

任务 1.2、1.3 与 1.5 依赖后续真实服务端周期，但不阻塞 2.x–5.x 的安全接缝开发。真实证据不足期间，只有明确已知结构可以分类，其余必须返回 unknown，且写请求不可到达。

2026-07-23 V2EX 子门禁增量：授权 Web Session 已完成“可领取页面 → 唯一 `input onclick` 动作 → 已领取结果 → 独立再次访问仍已领取”的真实安全链，并以不可重放占位值提交 observed-sanitized Fixture。production policy 现只允许严格 `/mission/daily/redeem`、唯一非空 `once` 和有限 `location.href = '<URL>';` 语法。由于真实失效会话跳转、完整重定向链与 NGA 完整链仍缺失，1.3、1.5 和 6.4 保持未完成。

## 2. NGA 签到适配

- [x] 2.1 基于明确标注来源的 Fixture 实现 NGA 签到统计与写响应的纯解析器，对未签到、已签到、成功、受限、鉴权失败和未知结构进行严格分类；合成未签到结构不得授权生产写入
- [x] 2.2 为 NGA 签到状态和写动作实现可注入请求服务，复用现有 Cookie 会话及项目网络约定，并保证只有明确未签到状态能够到达写请求
- [x] 2.3 为 NGA 请求构造、Fixture 解析、先读后写、重复已签到、未知响应 fail-closed 和网络错误分类添加单元测试

## 3. V2EX 签到适配

- [x] 3.1 基于明确标注来源的 Fixture 实现 V2EX 每日奖励 HTML 解析器，严格区分可领取、已领取、登录跳转和未知页面；合成可领取结构不得授权生产写入
- [x] 3.2 实现 V2EX 领取动作校验，要求 HTTPS、严格 `www.v2ex.com` 主机、经 Fixture 确认的路径和非空动态 `once`，并拒绝跨域、错误 topic/path 及意外参数形状
- [x] 3.3 为有效 Web Session 实现可注入的页面检查、至多一次领取和领取后二次确认服务；Token-only 会话不得发送网页签到请求
- [x] 3.4 为 V2EX Fixture 解析、恶意/意外链接拒绝、Token-only、Cookie 失效、已领取和二次确认失败添加单元测试

## 4. 协调、偏好与状态

- [x] 4.1 定义不暴露原始来源字段的共享签到结果与安全状态摘要，使来源服务可表达刚签到、已签到、会话不合格、可重试失败和当前窗口停止
- [x] 4.2 实现 NGA/V2EX 独立且默认关闭的自动签到偏好，并验证持久化不会包含凭证或把一个来源的选择应用到另一个来源
- [x] 4.3 实现签到协调对象的每来源 in-flight 去重、启动/前台节流、成功周期缓存、失败退避和会话变更清理，保持服务端状态为最终权威
- [x] 4.4 使用可控时钟和假服务覆盖并发启动、短时间重复 active、多设备已签到、失败重试、账号切换及 V2EX UTC 日界线；NGA 周期只采用任务 1 已证实的规则

## 5. 生命周期与账户体验

- [x] 5.1 在 `AuthSessionRegistry.restoreAll()` 完成后接入独立签到协调入口，使冷启动和经过节流的前台恢复可触发检查，但不阻塞 Feed 加载
- [x] 5.2 验证来源切换、Feed 刷新和 LINUX DO 不触发签到，并确保 UI Test 场景不访问真实签到端点
- [x] 5.3 在账户界面增加 NGA/V2EX 独立开关、最近安全结果和 V2EX 需要网页登录的操作提示，不显示 Cookie、Token、动态参数或未经确认的账号归属
- [x] 5.4 验证成功、已签到和普通网络失败不产生启动弹窗，离线、超时、5xx 与未知响应不会单独把来源会话改为 expired

## 6. 文档与验证

- [x] 6.1 更新 `docs/modules/account-and-session.md`、`docs/modules/nga.md`、`docs/modules/v2ex.md`、`docs/modules/feature-matrix.md`、`docs/decisions.md` 和 `docs/changelog.md`，说明 opt-in、Web Session 限制、前台触发、非后台保证及 fail-closed 合约
- [x] 6.2 更新 `docs/todo.md` 的变更状态；在真实响应、自动化测试和真机验证全部满足前不得标记完成
- [x] 6.3 运行相关解析、服务、协调和偏好测试，并检查测试日志与 Git Diff 不包含凭证、账号标识、原始响应或动态令牌
- [ ] 6.4 按项目规范在已连接 iOS 真机完成构建，并使用测试账号人工验证 NGA/V2EX 未签到、已签到、重复前台、离线和会话失效路径；没有真机时明确记录未执行且不以模拟器替代

验证记录（2026-07-22）：已在真机 `A67DE9B5-C985-51C3-83BE-FBC006C114A4` 完成聚焦自动签到测试（17 项）与完整 `ForumHubTests`，均通过；strict Rasen 校验、敏感信息模式扫描与 `git diff --check` 通过，因此 6.3 已完成。用户已手动成功安装到真实设备，但 NGA/V2EX 未签到、已签到、重复前台、离线和会话失效路径尚未完成账号人工验证，真实未完成与成功样本也仍待后续服务端周期补齐，因此 1.2、1.3、1.5 与 6.4 保持未完成，production 写请求继续不可达。

采样记录（2026-07-23）：在同一已授权 NGA 真机会话中，DEBUG-only 一次性探针确认 `get_stat` 最终 URL 为 HTTPS 官方 `bbs.nga.cn/nuke.php`，HTTP 200，MIME 为 `text/javascript`，响应含服务端 `Date`。多次读取分别出现精确 JSON `null` 与顶层 `data/time` 统计结构；统计结构包含 `last_day`、`now_day` 等字段，但原始数值、UID、余额及其他账号内容均未输出或提交。由于响应在相邻读取间不稳定，且尚未稳定证明日期字段与服务端当前周期的编码关系，本轮没有执行 `check_in` POST，也没有写后二次确认；一次性探针已删除。任务 1.2、1.5 与 6.4 继续保持未完成，NGA production 写入门禁不解除。

采样记录（2026-07-27）：用户确认授权账号处于未签到状态后，在真机现有 Cookie 会话中以 DEBUG-only 一次性探针按本地 App API 文档请求 HTTPS `bbs.nga.cn/app_api.php`。`get_stat`、至多一次 `check_in` 和写后 `get_stat` 均返回 HTTP 200、`text/json`、服务端 `Date`，并具有相同的顶层 `code/msg/result` 结构、业务 `code=2` 和数组型 `result`；三次响应长度相同，未出现签到成功或状态变化证据。随后只读复查仍为相同结构。原始正文、Cookie、UID/CID、用户名与余额均未输出或提交，一次性探针已删除并重新安装无探针 Debug 包。该结果证明 `app_api.php` 可达，但当前请求仍缺少尚未确认的客户端、方法、请求头或其他合约条件；任务 1.2、1.5 与 6.4 继续未完成，NGA production 写入保持关闭。
