## 1. 基线与契约

- [x] 1.1 为 NGA 秒/毫秒时间戳、V2EX 时间戳、Discourse ISO 8601、旧快照字符串和不可解析时间补充脱敏 Fixture 与失败测试
- [x] 1.2 为 `checking`、`signedOut`、`authenticated`、`expired` 及网络/403/5xx 非失效路径补充会话状态测试
- [x] 1.3 审计并验证各来源的新主题 capability 与目的地，记录 NGA 是否有可复用入口；未验证来源必须确定为隐藏或明确不支持，禁止继续空动作

## 2. 结构化时间与迁移

- [x] 2.1 在共享领域层建立可空的结构化帖子时间和单一解析/格式化接缝，覆盖 Unix 秒、毫秒、常见 NGA 日期与 ISO 8601
- [x] 2.2 修改 NGA、V2EX 和 LINUX DO Adapter/Parser，使创建时间和最后回复时间在来源边界转成结构化值，不再输出来源格式化字符串作为权威值
- [x] 2.3 为历史、收藏及其他持久化快照实现旧字符串时间兼容解码和稳定的新格式写入，确认不可解析时间不会丢弃帖子
- [x] 2.4 让 Feed 排序和帖子行共同消费结构化时间：最后回复模式显示“回复 MM-dd HH:mm”，最新发帖模式显示“发布 MM-dd HH:mm”，备用字段标签与实际字段一致
- [x] 2.5 增加时间解析、跨来源一致格式、时区、排序、备用字段和原始技术值不泄漏的单元测试

## 3. 类型化会话与动作续接

- [x] 3.1 扩展安全会话描述为 `checking`、`signedOut`、`authenticated`、`expired` 类型化状态，并确保不包含 Cookie、Token、CID 或原始鉴权响应
- [x] 3.2 把来源明确的 401/无效会话证据连接到 `expired`，同时保证离线、超时、限流、5xx 和普通 403 保持原错误与原会话状态
- [x] 3.3 在首页实现条件式会话提示：认证成功完全隐藏，未登录显示“登录”，失效显示“重新登录”，检查中不闪现 CTA
- [x] 3.4 建立来源/频道绑定的 pending compose action，登录成功后重新验证上下文并续接，取消登录、切换来源或无效目的地时清除
- [x] 3.5 把首页发帖按钮接入已验证 capability；支持来源进入有效目的地，不支持来源隐藏或给出明确说明，删除 `onCompose: {}` 空动作
- [x] 3.6 增加会话转换、错误分类、敏感信息隔离、条件式 UI 和登录后发帖续接测试

## 4. 统一筛选与偏好作用域

- [x] 4.1 建立 `FeedFilterState` 及版本化偏好存储，按来源记忆排序/置顶，按来源与父版记忆子版，并在恢复时清理无效子版
- [x] 4.2 用统一筛选 Sheet 替换分散的子版与置顶按钮，提供多选、置顶 Toggle、激活数量、重置、取消和应用
- [x] 4.3 让筛选 Sheet 使用草稿状态并在“应用”时一次性提交，验证多个条件变化最多触发一次必要 Feed 重新加载
- [x] 4.4 区分论坛真空列表和筛选无结果状态；筛选无结果必须显示激活数量及一键重置
- [x] 4.5 增加筛选数量、批量应用、作用域隔离、无效子版清理、置顶显示和空结果恢复测试

## 5. 首页信息架构与折叠

- [x] 5.1 按来源、搜索/刷新/发帖、频道、排序/筛选、条件式会话提示、帖子列表的顺序重组首页，并删除重复的当前频道行
- [x] 5.2 将“最后回复 / 最新发帖”改为明确的互斥分段控件，保持排序切换即时且不改变筛选
- [x] 5.3 实现带滞回阈值的顶部折叠与恢复：折叠来源/搜索动作区，持续保留频道、排序和激活筛选上下文
- [x] 5.4 统一显式刷新和下拉刷新到现有 generation/cancellation 链路，验证两种入口不会产生 stale 覆盖或重复帖子
- [x] 5.5 补齐搜索、刷新、发帖、频道、排序、筛选和登录动作的稳定 accessibility identifier、状态化 label、最小点击区域和动态字体布局
- [x] 5.6 增加首页层级、重复频道移除、头部折叠/恢复、横滑频道、下拉刷新和窄屏/动态字体 UI 测试

## 6. 文档与完整验收

- [x] 6.1 更新 `docs/modules/forum-feed.md`、会话/领域相关文档、`docs/decisions.md`、`docs/changelog.md` 和 `docs/todo.md`，记录信息架构、会话状态、筛选作用域与结构化时间决策
- [x] 6.2 运行时间、会话、筛选、Feed generation/cancellation 和首页 UI 的聚焦测试并修复全部失败
- [x] 6.3 在已连接且已解锁真机目标运行完整 `ForumHubTests` 和 Debug 构建，不得用仅编译通过替代测试
- [x] 6.4 安装到真机验收 NGA、V2EX、LINUX DO 首页时间，正常登录隐藏、未登录“登录”、失效“重新登录”，筛选应用/重置、置顶、排序、折叠、刷新及发帖入口
- [x] 6.5 在真机使用大号动态字体与 VoiceOver 检查控件可达性、焦点顺序、折叠后隐藏元素和底部栏遮挡
- [x] 6.6 检查 `git diff`、敏感信息、Fixture 脱敏、无关修改和调试代码，所有自动化及人工验收完成后才将 change 标记完成
