## 1. NGA 表情目录

- [x] 1.1 在 NGA 来源适配范围内建立统一回复表情目录，承载六组标识、标题、文件名、URL 与反向识别，保持既有三组条目兼容
- [x] 1.2 按两位补零规则加入 `pt00...pt64`、`dt01...dt33`、`pg01...pg15`，并确保目录成员不依赖运行时网络探测
- [x] 1.3 将 `NGAForumEmojiItem`、快捷表情来源和 `ReplyComposerEmoji` 恢复逻辑迁移到统一目录，继续输出原有 `[img]URL[/img]` 标记

## 2. 目录与文档测试

- [x] 2.1 为潘斯特、外域、企鹅增加首尾文件名、数量、唯一性和 URL 生成测试
- [x] 2.2 为六组已知文件名和未知图片文件名增加正反向识别测试
- [x] 2.3 扩展 `ReplyComposerDocument` 测试，覆盖新增三组在当前选择位置插入、连续插入、序列化与恢复往返
- [x] 2.4 运行相关单元测试并确认既有 NG娘、AC娘 v1、AC娘 v2 与普通文本编辑用例无回归

## 3. 单面板回复交互

- [x] 3.1 为回复编辑器增加明确的键盘/表情展示状态，在切换时保留回复目标、文档选择位置和附件
- [x] 3.2 移除嵌套 `NGAEmojiPickerSheet`，在原回复 Sheet 工具栏下方加入可展开的内嵌表情区域
- [x] 3.3 用横向滚动分类栏展示 `NG娘、AC娘 v1、AC娘 v2、潘斯特、外域、企鹅`，提供清晰选中态和完整无障碍标签
- [x] 3.4 使用稳定尺寸的懒加载表情网格支持连续选择，移除开发阶段说明文案并为单项加载失败提供局部占位
- [x] 3.5 调整正文聚焦、键盘收起和 Sheet 高度协作，确保点击正文恢复输入且切换过程中不重建回复内容

## 4. 视觉与边界状态

- [x] 4.1 使用现有 `PaperTheme` 与 `ForumGlass` 整理标题、正文、工具栏、分类栏和发布动作层级，不引入新的独立视觉体系
- [x] 4.2 验证空正文、提交中、取消发布并关闭、楼层目标切回主题、九附件上限和附件加载失败状态保持现有业务约束
- [ ] 4.3 验证小屏 iPhone 在键盘可见与表情面板可见时不存在文字截断、控件重叠或发布动作不可达（用户决定暂不验证，不阻塞本次提交）
- [x] 4.4 检查 VoiceOver 标签、按钮点击区域、动态字体和加载占位不会造成布局跳动

## 5. 验证与文档

- [x] 5.1 在已连接 iOS 真机上构建 `ForumHub` 并运行回复编辑与 NGA 表情相关聚焦测试；无真机时如实记录未执行
- [ ] 5.2 在真机人工验证主题回复、楼层回复、键盘/表情切换、六组浏览、连续插入、图片附件和发布中状态（主题回复的输入、发布与关闭已确认正常；其余矩阵待验证，不阻塞本次提交）
- [x] 5.3 根据实际实现同步 `docs/modules/thread-detail.md`、`docs/modules/nga.md`、`docs/changelog.md` 和受影响的 `docs/todo.md`
- [x] 5.4 执行 `git diff` 检查，确认未改变其他数据源能力、NGA 回复端点或图片上传协议，且未遗留调试代码
