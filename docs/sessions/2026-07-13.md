# 2026-07-13 迁移验证记录

## SD-3.1 收敛滚动状态所有权

- 日期与时间：2026-07-13 04:01（Asia/Shanghai）
- 变更范围：`ThreadDetailPaginationState`、`ThreadDetailScrollState`、`ThreadDetailViewModel`、`ThreadDetailView` 及相关测试和模块文档
- 设备：哥谭之王，iPhone Air（iPhone18,4），`00008150-001A4D5E1428401C`，网络连接
- 系统与构建：iOS 27.0（24A5380h），Debug，Xcode beta 工具链
- 测试命令：`xcodebuild -project ForumHub.xcodeproj -scheme ForumHub -configuration Debug -destination 'platform=iOS,id=00008150-001A4D5E1428401C' test -only-testing:ForumHubTests -only-testing:ForumHubUITests/ForumHubUITests/testThreadDetailScrollAutoAdvancesToNextPage -only-testing:ForumHubUITests/ForumHubUITests/testThreadDetailScrollToTopReturnsToFirstPage`
- 结果：通过；完整 `ForumHubTests` 及 2 个指定 Mock UI Test 均为 0 失败。
- 人工验证：本切片未单独执行真实页面人工验证；UI 行为由固定多页 Mock 场景覆盖。
- 已知限制：没有重新执行真实 NGA 会话；只看楼主、倒序和图片高度变化未新增独立 UI Test，继续由既有共享滚动链路与单元测试约束。
- 回滚点：删除 `ThreadDetailScrollState.swift`，并将四个滚动派生字段及 View 绑定恢复到 `ThreadDetailPaginationState`；不影响 Repository 和领域数据模型。

## SD-2.2 删除 body 独立存储

- 日期与时间：2026-07-13 04:05（Asia/Shanghai）
- 变更范围：`ForumThread`、`Reply`、内容权威来源回归测试及 ADR/上下文文档
- 设备：哥谭之王，iPhone Air（iPhone18,4），`00008150-001A4D5E1428401C`，网络连接
- 系统与构建：iOS 27.0（24A5380h），Debug，Xcode beta 工具链
- 测试命令：`xcodebuild -project ForumHub.xcodeproj -scheme ForumHub -configuration Debug -destination 'platform=iOS,id=00008150-001A4D5E1428401C' test -only-testing:ForumHubTests`
- 结果：通过；完整 `ForumHubTests` 为 0 失败，新增主楼和回复只读投影测试通过。
- 人工验证：未执行；本次不改变 `body` 的对外读取值，且没有用户界面结构调整。
- 已知限制：初始化器仍保留 `body` 兼容参数，调用点尚未统一改为直接构造 `ForumPostDocument`；这是减少迁移面的有意选择。
- 回滚点：将 `ForumThread.body`、`Reply.body` 恢复为存储属性，并在初始化器恢复 `self.body = body`；无需迁移收藏或历史数据。

## SD-2.3 评估 ThreadSummary 与 ThreadDetail 分离

- 日期与时间：2026-07-13（Asia/Shanghai）
- 变更范围：Repository、Feed、Search、Navigation、ThreadDetail、Favorites、History 调用链审计；ADR-012
- 设备：不适用（只读架构评估与文档变更）
- 系统与构建：不适用
- 测试命令：未执行；没有生产代码、接口或运行行为变更
- 结果：评估完成，当前否决拆分；重新评估的证据门槛已写入 ADR-012。
- 人工验证：通过源码搜索核对三个数据源 Repository、主要 Feature、详情状态和两个持久化快照的 `ForumThread` 消费路径。
- 已知限制：运行期仍由同一模型表达摘要与详情，必须继续保留 placeholder 和正文权威来源回归测试。
- 回滚点：删除 ADR-012、会话记录和对应待办结论即可；无生产代码需要回滚。

## SD-7.1 持久化版本、损坏降级和迁移

- 日期与时间：2026-07-13（Asia/Shanghai）
- 变更范围：Favorites、History、BlockedUsers、ChannelSubscription、本地快照编码工具、相关测试和持久化文档
- 设备：哥谭之王，iPhone Air（iPhone18,4），`00008150-001A4D5E1428401C`，网络连接
- 系统与构建：iOS 27.0（24A5380h），Debug，Xcode beta 工具链
- 测试命令：`xcodebuild -project ForumHub.xcodeproj -scheme ForumHub -configuration Debug -destination 'platform=iOS,id=00008150-001A4D5E1428401C' test -only-testing:ForumHubTests`
- 结果：通过；完整 `ForumHubTests` 为 0 失败，旧裸数组迁移、损坏快照降级和非法订阅键清洗用例通过。
- 人工验证：未执行；没有页面布局变化，持久化行为由隔离 UserDefaults suite 覆盖。
- 已知限制：损坏快照降级为空，不提供内容级修复；未来 schema version 必须新增显式迁移分支和测试。
- 回滚点：恢复三个 Store 的裸数组 JSON 编解码，删除 `VersionedLocalSnapshot.swift`，并移除订阅 schema version/键清洗；现有 v1 信封数据在回滚版本中将按损坏数据降级，因此发布后回滚需同时提供反向迁移。
